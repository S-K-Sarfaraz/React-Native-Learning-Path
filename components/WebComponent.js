import React from 'react';
import {WebView} from 'react-native-webview';

const WebComponent = () => {
  return <WebView source={{uri: 'https://in.pinterest.com/'}} />;
};

export default WebComponent;
